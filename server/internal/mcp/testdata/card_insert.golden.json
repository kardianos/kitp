{
  "type": "object",
  "properties": {
    "assign_to_me": {
      "type": "boolean",
      "description": "after insert, route the card to the calling user's inbox: agents self-route via user_card_agent; humans get assignee set to their linked person card"
    },
    "attributes": {
      "type": "object",
      "description": "optional map of additional attribute name to JSON value",
      "additionalProperties": true
    },
    "card_type_name": {
      "type": "string",
      "description": "name of the card_type to create (e.g. project, task)"
    },
    "parent_card_id": {
      "type": "string",
      "description": "parent card id; nil for top-level project cards"
    },
    "phase": {
      "type": "string",
      "description": "initial phase for value-cards; one of triage|active|terminal (defaults to triage)"
    },
    "title": {
      "type": "string",
      "description": "value for the built-in title attribute"
    }
  },
  "required": [
    "card_type_name",
    "title"
  ]
}
